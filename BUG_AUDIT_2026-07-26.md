# گزارش بررسی کامل کد - باگ‌های منطقی (۲۶ ژوئیه ۲۰۲۶)

بررسی شده توسط ۴ ایجنت جدا (بک‌اند اصلی، بات تلگرام، لایه‌ی احراز هویت/سلسله‌مراتب/تنظیمات، فرانت‌اند). هر مورد با فایل/خط دقیق، سناریوی واقعی شکست، و درجه‌ی اطمینان مشخص شده. مرتب‌شده از مهم‌ترین به کم‌اهمیت‌ترین.

---

## 🔴 امنیتی / فوری

### 1. اندپوینت `/api/ha/snapshot` با هر کلید API فعالی کار می‌کنه، نه فقط کلید مخصوص HA
**فایل:** `backend/app/routers/panel_settings.py` خط ۱۶۴-۱۶۷

```python
@ha_router.get("/snapshot")
def ha_snapshot(_: models.ApiKey = Depends(get_bot_api_key)):
    data = backup_service.create_snapshot_bytes()
    return Response(content=data, media_type="application/gzip")
```

`get_bot_api_key` فقط چک می‌کنه کلید معتبر و فعال باشه - هیچ چک نمی‌کنه که این کلید همون `PanelSettings.ha_peer_api_key` هست یا نه. یعنی **هر کلید API که برای هر مقصود دیگه‌ای ساختی** (مثلاً کلید یه ربات فروش شخص ثالث، یا کلید خودکار ربات سرور دوم) می‌تونه با همون کلید کل دیتابیس پنل (شامل پسورد هش‌شده‌ی همه‌ی ادمین‌ها، اطلاعات SSH نودها، خود `ha_peer_api_key`) رو دانلود کنه.
**اطمینان: بالا.** فیکس: چک کردن که کلید دقیقاً برابر `PanelSettings.ha_peer_api_key` باشه، نه هر کلید فعالی.

---

## 🟠 اولویت بالا

### 2. کانتر مصرف/بالانس به‌صورت race-condition-پذیر آپدیت می‌شه (نه اتمیک)
**فایل:** `backend/app/services/quota_manager.py`، تابع `_apply_delta` خط ۵۳-۷۳

مصرف کاربر/پکیج (`used_bytes`) و موجودی حجمی ادمین (`admin.volume_balance_gb`) با یه `read -> +delta -> write` معمولی پایتونی آپدیت می‌شن، نه با یه `UPDATE` شرطی اتمیک (مثل چیزی که برای کیف پول مشتری استفاده شده - `routers/users.py`'s `_charge_admin_for_package`). چون polling (هر ۳۰ ثانیه) و RADIUS accounting هر کدوم ترد/سشن دیتابیس جدا دارن و هم‌زمان می‌تونن روی یه کاربر (که هم WireGuard هم OpenVPN داره) بنویسن، یکی از دو آپدیت می‌تونه گم بشه (lost update) - خصوصاً روی `volume_balance_gb` که مستقیم به درآمد ادمین‌ها مربوطه.
**اطمینان: بالا.**

### 3. اندپوینت‌های «ریست مصرف» (کاربر و خرید) وضعیت رو active می‌کنن بدون چک انقضا
**فایل:** `backend/app/routers/users.py` خط ۶۰۴-۶۱۷ (کاربر) و ۷۵۰-۷۷۰ (خرید مستقل)

دقیقاً همون کلاس باگی که تو «تمدید سریع» پیدا کردیم (که هنوز باید براش تصمیم بگیریم) - اینجا هم اگه کاربر/خریدی هم‌زمان هم حجمش تموم شده هم تاریخش گذشته (که خیلی معمول هست)، دکمه‌ی «ریست مصرف» فقط `used_bytes` رو صفر می‌کنه و وضعیت رو active می‌کنه و اتصال رو زنده می‌کنه - ولی چون تاریخ انقضا دست‌نخورده مونده، ظرف کمتر از ۳۰ ثانیه (`POLL_INTERVAL_SECONDS`) دوباره expired می‌شه و قطع می‌شه.
**اطمینان: بالا.**

### 4. فعال‌سازی «تمدید رزروشده» یه بار اتصال واقعی VPN رو زنده می‌کنه قبل از اطمینان از هر دو بُعد (حجم + تاریخ)
**فایل:** `backend/app/services/user_ops.py` خط ۳۵۶-۳۸۹ و ۱۰۱۶-۱۰۳۸، مسیر لایو: `backend/app/services/radius_server.py` خط ۳۱۲-۳۳۴

وقتی یه تمدید فقط حجم رو رزرو کرده (نه روز)، و بعداً کاربر از نظر تاریخ منقضی می‌شه و تلاش برای اتصال (لاگین RADIUS) می‌کنه، این تابع رزرو رو فعال و `status=active` می‌کنه و واقعاً (با API میکروتیک/Xray) اتصال رو روشن می‌کنه - قبل از اینکه دوباره چک بشه که تاریخ هنوز گذشته. لاگین خودش رد می‌شه ولی اتصال چند لحظه‌ای واقعاً باز شده تا poll بعدی دوباره ببندتش.
**اطمینان: بالا (رفتار کد)، متوسط (احتمال رخ دادن در دنیای واقعی).**

### 5. `renew_purchase` همون باگ ناهمگونی حجم/روز رو داره که `renew_user` داشت
**فایل:** `backend/app/services/user_ops.py` خط ۹۶۴-۱۰۱۳

عیناً همون سناریوی «تمدید سریع کار نمی‌کنه» ولی برای خریدهای مستقل (Purchase، نه کاربر). اگه فیکسی برای این کلاس باگ می‌زنیم، باید اینجا هم اعمال بشه.
**اطمینان: بالا.**

### 6. حذف/جابه‌جایی ادمین، مالکیت پکیج/آموزش رو منتقل نمی‌کنه - رکوردهای یتیم و نامرئی برای همه
**فایل:** `backend/app/routers/admins.py`، `delete_admin` خط ۵۷۳-۶۰۹ و `reparent_admin` خط ۲۶۱-۳۱۵

وقتی یه ادمین حذف یا جابه‌جا می‌شه، کاربرهاش درست منتقل/یتیم می‌شن (و یتیم‌ها برای سوپرادمین قابل مشاهده‌ان) ولی پکیج‌ها/آموزش‌های اون ادمین دست‌نخورده با `owner_admin_id` قدیمی (که دیگه هیچ‌کس نیست) باقی می‌مونن - نه سوپرادمین می‌بینتشون نه هیچ ادمین دیگه‌ای، برای همیشه.
**اطمینان: بالا.**

### 7. بات تلگرام: وقتی مشتری خرید رو نصفه‌کاره رها می‌کنه و دوباره از دکمه (نه از `/buy`) شروع می‌کنه، state کد تخفیف/دعوت قبلی می‌مونه
**فایل:** `backend/app/telegram_bot/handlers/customer.py` خط ۵۷۰-۵۸۵ (`cb_buy`/`cb_renew`) در مقابل خط ۲۴۴-۲۶۳ (`cmd_buy` که درست `state.clear()` می‌کنه)

دکمه‌های «خرید اکانت جدید»/«تمدید سرویس» (که ۹۹٪ استفاده مسیرشونه) برخلاف دستور `/buy`، وضعیت قبلی رو پاک نمی‌کنن. اگه مشتری یه کد تخفیف رو تو یه خرید نصفه‌کاره اعمال کرده باشه و بعد از یه پیام «🏠 خانه»ی قدیمی دوباره خرید جدید بزنه، ممکنه همون کد تخفیف (بدون اعتبارسنجی دوباره روی پکیج/قیمت جدید) رو قیمت خرید تازه اعمال بشه.
**اطمینان: بالا.**

### 8. تنظیم «آیتم‌های مخفی منوی مشتری» فقط برای ۲ آیتم (تمدید/شارژ) واقعاً اجرا می‌شه، نه ۸ تای دیگه
از ۱۰ آیتم قابل‌مخفی‌کردن (`cust_account`, `cust_usage`, `cust_renew`✅, `cust_buy`, `cust_topup`✅, `cust_tutorials`, `cust_referral`, `cust_support`, `cust_link`, `cust_myid`)، فقط `cust_renew` و `cust_topup` رو همین جلسه فیکس کردیم (که دستورشون واقعاً بلاک بشه، نه فقط دکمه مخفی بشه). بقیه (خصوصاً `cust_buy` با دستور `/buy`، و `cust_link` با دستور `/link`) همچنان فقط دکمه‌شون مخفیه، نه خود قابلیت.
**اطمینان: بالا.** اگه می‌خوای همه رو یکسان کنم بگو.

### 9. یادآوری: فیکس «دسترسی مشتری‌ها به ربات» (که همین جلسه ساختیم) هنوز روی سرور اجرا/کامیت نشده
هر دو ایجنت مستقل (بک‌اند بات + لایه‌ی تنظیمات) این باگ رو دوباره پیدا کردن، چون کدش رو سرور شما هنوز همون قبلیه - اسکریپت `fix_customer_access_toggle.py` که قبلاً ساختم رو باید هنوز رو سرور اجرا و کامیت کنی (یادت رفت وسط بحث پکیج‌ها).

### 10. `telegram_api_proxy_url` (پراکسی تلگرام) هم دقیقاً همون مشکل رو داره - به بات روی سرور دوم اصلاً نمی‌رسه
**فایل:** `backend/app/telegram_bot/runner.py` خط ۱۸۴-۲۰۲، `backend/app/services/remote_deploy.py` خط ۱۰۶-۱۱۶

بات روی سرور دوم دیتابیس محلی خالی خودش رو داره، پس این تنظیم رو هیچ‌وقت نمی‌بینه - اگه برای دور زدن فیلترینگ تلگرام تنظیمش کرده باشی، رو سرور دوم اصلاً اعمال نمی‌شه.
**اطمینان: بالا.**

---

## 🟡 اولویت متوسط

- **پکت‌های RADIUS نامرتب (Start بعد از Stop قدیمی) می‌تونن مصرف رو غلط دوبار حساب کنن** - `radius_server.py` خط ۵۳۵-۵۵۵.
- **اگه فعال‌سازی پکیج (تکی، نه گروهی) کامل شکست بخوره، پول ادمین برنمی‌گرده** - برخلاف مسیر گروهی که refund داره. `routers/users.py` (`create_user`/`apply_package`) + `user_ops.py`'s `apply_package_as_purchase`.
- **`panel_bridge.py` و `remote_bridge.py` (پل بات) کاملاً آینه‌ی هم نیستن** - چند متد تو نسخه‌ی ریموت پارامتر `owner_admin_id`/`_scope()` رو اصلاً نداره. فعلاً چون فقط بات مشترک (global) رو سرور دوم نصب می‌شه بی‌خطره، ولی اگه بعداً بات اختصاصی هر ادمین رو سرور دوم قابل‌نصب کنی، این می‌ترکه.
- **خطاهای غیرمنتظره (نه ApiError) تو تایید رسید، درخواست رو برای همیشه گیر می‌ندازه رو حالت «در حال بررسی»** - `admin_pending.py` خط ۱۰۹-۲۶۶.
- **HA: اگه دستی نقش سرور standby رو عوض کنی (بدون فیل‌اور خودکار)، سرویس‌های اصلی تا ری‌استارت بالا نمیان** - `main.py` خط ۲۹۰-۳۱۹ + `ha_tick`.
- **دکمه‌ی «بازگرداندن ربات به سرور اصلی» کلید API ربات سرور دوم رو غیرفعال نمی‌کنه** - می‌مونه فعال برای همیشه. `routers/remote_bot.py`'s `stop()`.

---

## 🟢 فرانت‌اند (پنل وب)

### 11. دکمه‌ی «تمدید این خرید» (سطح هر Purchase، نه کل کاربر) دقیقاً همون باگ «تمدید سریع» رو داره
**فایل:** `frontend/src/pages/UserDetail.jsx` خط ۳۵۷-۳۸۹ (`openRenewPurchase`)

### 12. مودال «کاربر جدید» هیچ‌وقت فرمش ریست نمی‌شه
**فایل:** `frontend/src/pages/Users.jsx` خط ۵۴۷ در مقابل ۲۴۶

برخلاف همه‌ی مودال‌های دیگه‌ی پروژه (که موقع باز شدن فرم رو ریست می‌کنن)، دکمه‌ی «کاربر جدید» هیچ ریستی نداره. اگه یه بار فرم رو پر کنی و Cancel بزنی (یا خطا بخوره)، دفعه‌ی بعد که باز می‌کنی همون مقادیر قبلی (حجم، روز، **و مهم‌تر: `owner_admin_id`**) هنوز اونجاست - می‌تونه باعث بشه کاربر جدید رو اشتباهی زیر ادمین/فروشنده‌ی غلط بسازی.
**اطمینان: بالا.**

### 13. بعد از حذف همه‌ی کاربرهای صفحه‌ی آخر، شماره صفحه خودش برنمی‌گرده
**فایل:** `frontend/src/pages/Users.jsx`

### 14. تیک‌های انتخاب‌شده موقع تغییر فیلتر/جستجو پاک نمی‌شن (بی‌سروصدا رو کاربرهای نامرئی هم اثر می‌ذاره)
**فایل:** `frontend/src/pages/Users.jsx`

---

## جمع‌بندی پیشنهادی

بیشترین ریسک/اولویت به نظرم: **#1 (امنیتی)**، **#9 (یادت رفته دیپلوی کنی)**، **#3 و #5 (همون کلاس باگ تمدید، جاهای دیگه)**، **#12 (فرم کاربر جدید ریست نمی‌شه - می‌تونه کاربر رو زیر ادمین اشتباه بسازه)**.

بگو کدوم‌ها رو فیکس کنم، به همون ترتیبی که می‌خوای براشون پچ می‌سازم (مثل قبل - قابل تست محلی، بعد اجرا رو سرور، بعد کامیت به گیت‌هاب).
